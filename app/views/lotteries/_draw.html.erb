<div class="my-form-inline init-fish-form">
<%= form_tag action: :choose  do %>
    <%= hidden_field_tag :draw_id, user.draw_id %>
    <%= submit_tag '', class: 'btn btn-primary draw' %>
<% end %>
</div>
